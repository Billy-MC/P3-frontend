import Header from './Header';
import SideNav from './SideNav';
import Footer from './Footer';

const Layout = () => (
    <div>
        <Header />
        <SideNav />
        <Footer />
    </div>
);

export default Layout;
