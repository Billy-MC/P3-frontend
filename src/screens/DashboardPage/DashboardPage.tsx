import LoadingSpinner from '../../components/LoadingSpinner';

const DashboardPage = () => (
    <div>
        <LoadingSpinner />
        <p>DashboardPage</p>
    </div>
);

export default DashboardPage;
